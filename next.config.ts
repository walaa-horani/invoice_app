import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Next.js 16 supports experimental features if needed, but standard config is clean.
};

export default nextConfig;
